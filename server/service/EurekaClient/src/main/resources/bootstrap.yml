eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

#server:
#  port: 8002

spring:
  application:
    name: eurake-client
  cloud:
    config:
      uri:  http://localhost:8888
  profiles:
    active: development